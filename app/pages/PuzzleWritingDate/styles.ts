import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ffffff',
  },
  chipsContainer: {
    marginTop: 8,
  },
  datePickerContainer: {
    marginTop: 12,
    marginLeft: 4,
  },
});
