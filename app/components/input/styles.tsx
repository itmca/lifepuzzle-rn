import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputDefaultStyle: {
    width: '100%',
    marginBottom: 8,
  },
  dateInput: {
    width: '100%',
    marginBottom: 0,
  },
});
