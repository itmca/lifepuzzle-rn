import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginLeft: 16,
    backgroundColor: '#ffffff',
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
  },
  numberInput: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});
