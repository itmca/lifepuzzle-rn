import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  animationText: {
    fontWeight: 'bold',
    lineHeight: 25,
    paddingBottom: 5,
  },
  fingerImage: {
    width: 24,
    height: 23,
  },
});
